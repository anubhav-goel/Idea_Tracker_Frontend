import React from "react";
import PropTypes from "prop-types";
import ButtonIcon from "../../../formInputs/ButtonIcon";

const TableBodyButtonIcon = props => {
	return <ButtonIcon {...props} />;
};

TableBodyButtonIcon.propTypes = {};

export default TableBodyButtonIcon;
